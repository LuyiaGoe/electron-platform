import { CapacitorConfig } from '@ura/capacitor-cli';

const config: CapacitorConfig = {
  appId: "com.example.myapp",
  appName: "ura-va",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
